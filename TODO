Date: 18 march

refine the student page coz revies and bookings dont show up correct info

Implement real payments flow
- Add backend endpoint to create Selcom (later DPO) transactions and return redirect URL/USSD/MPESA instructions
- Store Selcom API keys/merchant config in backend env
- Replace frontend PayPage optimistic success with gateway response handling
- Confirm reservation records are created upon successful payment

Next session
- Hide paid/occupied rooms from other users (decide rule: vacancy_status vs booking/payment status; allow owner/admin to see; apply to search/home/listing queries).

You need to manually add your OPENAI_API_KEY to 
supabase/functions/.env
 for local AI features to work.

in the mobile phone, the site keeps refreshing sometimes it becomes blank


##
After i pay, in phone it should show my room and a section dedicated to it. 
It should have info containinh the room etc payment records, due date rent, giving stars etc



##
edit the amenities to explain if a room has kitchen, single room etc


if a room is paid for, it should not be seen anymore ny another person since it is already paid for. Once i login as another person or when another person logs in they should not see the rooms that have already been paid for.


##admins page
admin should log in and the directly be taken to the landlords page


 NB:
 starting tthe backend service(supabase)
 supabase  start

 starting the frontend
 cd frontend &&npm run dev

 reseting the supabase cleaning everything locally
 supabase db reset


TOMORROW  13 may

follow up on the edit button
that is the error that happens when i press edit move out button. we need the notification to be sent to the landlord, now i think the landlord has no place to keep/receive the moveout text messages. We'll have to find a way cox the room will have to be posted as coming soon after it has been vacated. 
Failed to submit notice: Could not find the 'is_read' column of 'notifications' in the schema cache



User Review Required

Which of these areas feels like the highest priority to you right now?

For example, if you want the app to feel faster, we can start with Performance. If you want easier coding in the future, we can tackle Architecture. Or if you want to attract more users, we can focus on SEO & Social Sharing.